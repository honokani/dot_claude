# 共通ルール

## 行動原則
- 推測回答禁止
- おべっか・過剰な謝罪・言い訳・口答え禁止
- 動作確認なしに「成功」「完了」禁止

## 変更管理
- 既存ファイル変更前に削除理由の説明必須
- 既存機能は「削除していい」と明示されない限り残す（「追加」≠「置換」）
- 変更前に因果関係を説明する

## 設計判断
- 具体的な仕様指示はそのまま実装。代替案がある場合は実装前に提示→承認を得る
- 勝手な設計判断禁止

## 作業ステップ
1. 推論：影響分析→リスク評価→選択肢提示
2. 実行：承認後のみ
3. 検証：自己チェック必須

## 優先順位
- 満足度より技術正確性優先。チェック完了まで回答送信禁止

## コーディングスタイル
- Monadic（StateMonad等）で実装。関数連鎖は合成で実現
- **例外: in-place更新が性能上必要な場合は&mut直接操作優先**
  - ピクセルバッファ（Vec<[f32;4]>等）の毎フレーム新規生成、メモ化キャッシュ等
  - 基準: 「返す」コスト >> 「書き換える」コスト → モナディック回避
- モナディック適所: 値変換パイプライン（map/and_then）、データフロー評価チェーン、バリデーション
- twada的TDDを意識

## 開発管理
DESIGN.md・PROGRESS.md・KEYWORDS.mdの3ファイルを「開発管理」と呼ぶ。
- セッション開始時に読み込み、なければ初期化、あれば照合して更新
- 「開発管理の更新。」= 未反映の変更があれば3ファイルを更新せよ

### DESIGN.md
- 設計書。コンセプト・アーキテクチャ・レイアウト・キーバインド等
- 機能追加・変更時に更新。実装詳細ではなく設計意図と構造を記述

### PROGRESS.md
- チェックリスト形式の実装進捗。ステップごとに更新（高頻度）
- テスト累計数、決定ログも維持

### KEYWORDS.md
- トピックマーカー `[カテゴリ.トピック]` のインデックス。経緯は書かない
- PROGRESS.mdと同程度の頻度で更新。1トピック上限3行:
  ```
  ## [category.topic]
  - session: YYYY-MM-DDTxx-xx.jsonl / phase: PhaseX-Name
  - 概要: 1行
  ```
- Phase完了時 → `KEYWORDS_ARCHIVE.md` に移動（同一フォーマット）
- KEYWORDS.mdは進行中+直近完了Phaseのみ保持

## ~/.claude の変更管理
- DESIGN.md不要（CLAUDE.md自体が設計書）
- **GLOBAL_PROGRESS.md**: 設定変更ログ（日付+1行要約）
- **GLOBAL_KEYWORDS.md**: トピックマーカー索引（KEYWORDS.md同一フォーマット）
- ~/.claude 作業時・CLAUDE.md変更時に更新

## tips記録
- `~/.claude/tips/*.md`: プロジェクト横断の技術知見
- 関連技術を扱うとき自由に参照。躓いたら追記（なければ新規作成）
- 「tipsもあれば記載ください」= セッション中の知見をtipsに記録せよ

## workspace_for_claude
- `~/.claude/workspace_for_claude/`: Claudeの作業用。自由に使ってよい
- Python依存は `.venv/` 内で管理（グローバル汚染禁止、uvでインストール）
- Rustも同ディレクトリ配下で自由に使ってよい

## Skills
- 配置先: `~/.claude/skills/<skill-name>/SKILL.md`
- 繰り返しワークフロー（3ステップ以上）はSkill化を検討
- 断片的な知見は `tips/` に記録

## 割り込み作業とブランチ管理
- 割り込み発生時は git worktree を使う（stash禁止: 識別ミスリスク）
- 手順: WIPコミット → `git worktree add` → 完了後 `git worktree remove` → 必要に応じてmerge/rebase → `git reset HEAD~1` で再開
- Agent (`isolation: "worktree"`) で自動化可能
- 割り込みが現タスクの前提と判明 → 現ブランチを割り込みブランチ上にrebase（develop経由不要）
- PROGRESS.mdに中断スナップショット記録:
  ```
  ## Phase: feature-A [中断]
  中断理由 / 再開条件 / 再開起点 / 未決定事項
  ```

## デバッグ手法
- 推測修正より先にログ出力で事実確認。言語固有の手法は `tips/` に記録

## トピックマーカー
- 形式: `[カテゴリ.トピック]`（例: `[guard.pen_blocked]`, `[layout.layer_panel]`）
- 制御の追加・変更・削除時、マーカー付きで影響範囲を発言
- 後から検索したい議論・決定にもマーカー付与
- KEYWORDS.mdにインデックス記録。過去参照: sessionフィールドからJSONLをGrep
