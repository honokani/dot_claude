# 共通契約（全実行主体）

## 適用範囲
- 本ファイルは対話セッション（メイン）とサブエージェント（Agent/Workflow起動、モデル問わず）に共通で配布される契約。「対話セッションのルール」以下はユーザーと対話するメインのみに適用
- サブエージェントは委譲プロンプトを優先。ユーザー承認が要る判断は仮定を明示して進めるか、判断事項として結果に含めて返す（承認待ちで停止しない）

## 優先順位
- 満足度より正確性優先

## 報告と検証
- 事実と推測を区別して報告（推測には「推測」と明示）
- 「成功」「完了」の報告には検証根拠（テスト出力・実行結果・diff等）を添える。未検証は「未検証」と明示
- 結論先行・簡潔・事実ベース。失敗時はエラー原文・原因・対処のみ（弁明不要）
- 検証は外部経路（テスト・動作確認・hook）優先、自己チェックは補助

## 変更管理
- 既存ファイルの変更・削除前に理由を説明
- 既存機能は「削除していい」と明示されない限り残す（「追加」≠「置換」）
- 式・列挙等の構造で示された仕様は各要素が意図的。省略せず全要素を実装
- ファイル削除は不可（ハーネスがブロック）。削除が必要なら `mv` でプロジェクト直下の `_gomi/` へ退避（無ければ作成）。中身の削除はユーザーが行う

## 環境
- シェルコマンドは常にbash/POSIX構文（全環境にgit bash or zshあり。環境表記がPowerShellでもBashツールの実体はbash）。PowerShell必須の操作のみ `powershell -File` 経由
- Bashでのpython実行は `uv run python`（`python` 直打ち禁止、`pyenv` 操作禁止）。コード内に埋め込むパスはWindows形式（MSYS2形式 `/c/...` 不可）
- Bash失敗時にhookが注入する [traps-hint] は、修正前に該当 `~/.claude/traps/*.md` をRead
- セッション冒頭の「=== Previous session context (latest_cache) ===」ブロックは前セッションからの引き継ぎとして扱う
- `~/.claude/workspace_for_claude/` はClaudeの作業用。自由に使ってよい

## コーディングスタイル
- 言語問わず関数型スタイル。Monadicに合成、副作用は境界へ
  - 適所: 値変換パイプライン（map/and_then）、データフロー評価チェーン、バリデーション
  - 例外: 「返す」コスト >> 「書き換える」コスト → in-place更新（&mut直接操作）優先（例: ピクセルバッファ Vec<[f32;4]> の毎フレーム生成、メモ化キャッシュ）
- Pythonでも純粋関数優先・グローバル状態回避・パイプライン合成
- 繰り返し取得しうる処理はメモ化（API呼び出し・展開結果・計算結果。コードでも作業でも）
- twada的TDDを意識
- デバッグは推測修正より先にログ出力で事実確認

# 対話セッションのルール（メインのみ）

## 設計判断
- 仕様の所有権はユーザー。具体的指示はそのまま実装、代替案は実装前に提示→承認
- 未指定の設計判断は選択肢＋推奨を提示して承認を得る
- 技術的に同意できない指示は実行前に根拠付きで異議提示。ユーザー決定後は従う

## 作業ステップ
1. 着手前: 影響分析→リスク評価。非自明な選択肢は提示→承認
2. 実行: 指示範囲内かつ可逆な作業は進めて報告。不可逆・スコープ変更・新たな設計判断は承認後のみ
3. 検証: 「報告と検証」の基準（外部経路優先）

## 委譲
- bulk系依頼（「全件」「全項目」「全ファイル」「全テスト」等の「全X」）は自力処理前に必ず `bulk-verification` skill を invoke
  - Why: 項目単位の観測可能性確保。単一コンテキストの全件処理は網羅性を外部検証できない
- サブエージェント委譲のモデルは `~/.claude/MODEL_ROUTING.md` に従う（簡単・定型作業をメインモデルのまま委譲しない）

## pj管理
- VISION.md・PROGRESS.md・DECISIONS.md の3ファイル。セッション開始時に読込（なければ初期化、あれば照合して更新）。「pj管理の更新。」= 未反映の変更を3ファイルへ反映
- 3ファイル読込時、標準ディレクトリ `_from_owner/`（ユーザーからの受領物）・`_deliverables/`（会話用生成物）・`_gomi/`（削除退避）が無ければ作成（用途・git扱いは `pj-management` skill）
- 各ファイルの記録基準・タグ体系・テンプレート・初期化手順は `pj-management` skill（初期化・更新時に invoke）

## ブランチ運用
- 階層は **master → develop → feat系**。master=常に動くトランク、develop=統合ブランチ、開発は**短命フィーチャーブランチ**で隔離。リスク/実験的変更を直接master/developでやらない（壊れたコミットがmasterを汚し reset --hard 等の汚い後始末を招いた実績あり）
  - 命名: `feat/NN-<機能>-<趣旨>`（NN=対応issue番号、2桁ゼロ埋め。3桁になったらNNN）
  - 手順: developから切る→作業→**動作確認後にdevelopへmerge**（壊れたら破棄/やり直しがdevelop/masterに波及しない）→developで正常稼働を確認できたら都度masterへmerge。基本は逐次1本（PROGRESS/DECISIONS等は全機能共有で並行ブランチは衝突）
  - 例外: typo・即興の小修正はdevelop直で可
- 割り込みは git worktree（stash禁止: 識別ミスリスク）。Agent (`isolation: "worktree"`) で自動化可
  - 手順: WIPコミット → `git worktree add` → 完了後 `git worktree remove` → 必要に応じmerge/rebase → `git reset HEAD~1` で再開
  - 割り込みが現タスクの前提と判明 → 現ブランチを割り込みブランチ上にrebase（develop経由不要）
- 中断時はPROGRESS.mdに中断スナップショットを記録（形式は `pj-management` skill）

## 知見の記録
- エラー系の躓きを解決したら `~/.claude/traps/` へ追記or作成（エラーとして観測できる知見。命名 `<主題>_<細目>_<環境>.md`、主題語=hook照合キー。詳細は `traps/README.md`）。頻出の罠はCLAUDE.md無条件ルールへ昇格し、詳細をtrapsに残す
- `~/.claude/tips/*.md`（非エラー知見: 設計パターン・作業ノウハウ）の新規追加は保留中（扱い検討中）
- 繰り返しワークフロー（3ステップ以上）はSkill化を検討。テンプレート: `~/.claude/skills/TEMPLATE_SKILL.md`

## ~/.claude 自体の変更
- ~/.claude 配下（CLAUDE.md・skills・traps・hooks・settings等）を変更する時は `~/.claude/MAINTENANCE.md` に従う（実体repoとの同期pull/push・GLOBAL_*への記録・ルールの剪定基準）
