# 共通ルール

## 優先順位
- 満足度より正確性優先

## 行動原則
- 事実と推測を区別して報告（推測には「推測」と明示）
- 「成功」「完了」の報告には検証根拠（テスト出力・実行結果・diff等）を添える。未検証は「未検証」と明示
- 結論先行・簡潔・事実ベース。失敗時はエラー原文・原因・対処のみ（弁明不要）
- 技術的に同意できない指示は実行前に根拠付きで異議提示。ユーザー決定後は従う
- bulk系依頼（「全件」「全項目」「全ファイル」「全テスト」等の「全X」）は自力処理前に必ず `bulk-verification` skill を invoke
  - Why: 項目単位の観測可能性確保。単一コンテキストの全件処理は網羅性を外部検証できない

## 変更管理
- 既存ファイルの変更・削除前に理由を説明
- 既存機能は「削除していい」と明示されない限り残す（「追加」≠「置換」）

## 設計判断
- 仕様の所有権はユーザー。具体的指示はそのまま実装、代替案は実装前に提示→承認
- 未指定の設計判断は選択肢＋推奨を提示して承認を得る
- 式・列挙等の構造で示された仕様は各要素が意図的。省略せず全要素を実装

## 作業ステップ
1. 着手前: 影響分析→リスク評価。非自明な選択肢は提示→承認（大きな変更はPlan Modeで計画/実行を分離）
2. 実行: 指示範囲内かつ可逆な作業は進めて報告。不可逆・スコープ変更・新たな設計判断は承認後のみ
3. 検証: 外部経路（テスト・動作確認・hook）優先、自己チェックは補助

## コーディングスタイル
- 言語問わず関数型スタイル。Monadicに合成、副作用は境界へ
  - 適所: 値変換パイプライン（map/and_then）、データフロー評価チェーン、バリデーション
  - 例外: 「返す」コスト >> 「書き換える」コスト → in-place更新（&mut直接操作）優先（例: ピクセルバッファ Vec<[f32;4]> の毎フレーム生成、メモ化キャッシュ）
- Pythonでも純粋関数優先・グローバル状態回避・パイプライン合成
- 繰り返し取得しうる処理はメモ化（API呼び出し・展開結果・計算結果。コードでも作業でも）
- シェルコマンドは常にbash/POSIX構文（全環境にgit bash or zshあり。環境表記がPowerShellでもBashツールの実体はbash）。PowerShell必須の操作のみ `powershell -File` 経由
- Bashでのpython実行は `uv run python`（`python` 直打ち禁止、`pyenv` 操作禁止）。コード内に埋め込むパスはWindows形式（MSYS2形式 `/c/...` 不可）
- twada的TDDを意識
- デバッグは推測修正より先にログ出力で事実確認

## pj管理
VISION.md・PROGRESS.md・DECISIONS.md の3ファイル。セッション開始時に読込（なければ初期化、あれば照合して更新）。「pj管理の更新。」= 未反映の変更を3ファイルへ反映。

### VISION.md — ポールスター（更新: 機能追加・設計判断変更時、低頻度）
- プロジェクトの「あるべき姿」と「なぜそう作るか」。テンプレート: `~/.claude/TEMPLATE_VISION.md`
- 書く: Why（存在理由）／設計思想・不変条件／入出力情報／コード上の散在情報（キーバインド表等）
- 書かない（コードが正）: ファイル構成・API仕様・座標数値、実装手順（→PROGRESS）、判断経緯（→DECISIONS）
- 設計思想は3層: 全体ルール／機能横断ルール／機能別ルール（必要時のみ）
- ルールタグ: 各ルールにいずれか1つ付与
  - `[philosophy]`: テストで縛れない思想。VISION.mdが正
  - `[unittest]` / `[qchecktest]`: テストで検証する制約（タグ名=検証チャネル）。VISION.mdにはWhy、実装はテストコードが正
  - 旧 `[testable]` は廃止。既存ファイルで遭遇したら2タグへの振り分けを提案
- タグ更新規則（テスト作成時）: 作成できなかった項目は理由で分岐
  - 思想ゆえテスト不向き → `[philosophy]` へ更新（根拠をDECISIONS.mdへ1行）
  - 対象が未実装なだけ → タグ据置（実装時の1機能1テストループで回収）

### PROGRESS.md — 実行記録「何をしたか」（更新: ステップごと、高頻度）
- チェックリスト形式の実装進捗。テンプレート: `~/.claude/TEMPLATE_PROGRESS.md`
- 記録対象: 予定したPhase/タスクの進捗、既存挙動を変える/壊す改修（Phase未割当でも1行記録）
- 対象外: typo修正・即興の小変更（gitで追える）
- テスト累計数と決定ログ（実行記録）も維持
- Phase番号はセマンティックバージョニング準拠（初期構想完成=1.0.0）

### DECISIONS.md — 判断根拠「なぜAではなくBか」（更新: 判断発生時、中頻度）
- コードから読めない設計判断のログ。テンプレート: `~/.claude/TEMPLATE_DECISIONS.md`
- 記録対象: A/B選択の根拠／一見疑問に思われそうな実装の理由
- 対象外: 一意に決まる作業・typo修正
- 棲み分け: PROGRESS=何をしたか、DECISIONS=なぜそうしたか

## ~/.claude 自体の変更管理
- GLOBAL_VISION.md（設計思想）／GLOBAL_PROGRESS.md（変更ログ）／GLOBAL_DECISIONS.md（判断根拠）。記録基準はpj管理と同一
- ~/.claude 作業時・CLAUDE.md変更時に更新
- ルールの剪定: 行動ルールは「防ぐ失敗」とセットで管理。防いでいた失敗が直近セッションで再現しなくなったルールは、GLOBAL_DECISIONS.mdに記録して実験的に外す（根拠はモデルの自己申告でなく観測実績）

## 複数環境ファイルの同期運用
- dot_claude実体はgit repo（このWindows機では `C:/git_clone/dot_claude`）。~/.claude へは必要分のみsymlink。git操作は実体repo側で
- 編集前に `git pull`、編集後に `git push`（他環境へ即時反映）
- SessionEnd hookのauto-pushは失敗防止ネット。毎回のpushを省略しない

## latest_cache（セッション間コンテキスト引き継ぎ）
- SessionStart hook（session-start-cache.sh）が全自動処理: マーカー探索→鮮度判定→注入→old/移動。Claude側の手動確認は不要
- セッション冒頭に「=== Previous session context (latest_cache) ===」ブロックがあれば、前セッションからの引き継ぎとして扱う
- 設計思想は `GLOBAL_VISION.md` 参照

## tips記録
- `~/.claude/tips/*.md`: プロジェクト横断の技術知見。命名: `<主題>_<細目>_<環境>.md`（詳細は `tips/README.md`）
- 参照トリガー: 環境・ツール系の躓き（コマンド構文エラー・PATH・文字コード・改行等）が起きたら、修正を試みる前に `tips/` を主題語でgrep（既知の罠の再踏みが実際に多い）
- 躓きを解決したら追記or作成。セッション中の知見を記録せよ

## workspace_for_claude
- `~/.claude/workspace_for_claude/`: Claudeの作業用。自由に使ってよい

## Skills
- 繰り返しワークフロー（3ステップ以上）はSkill化を検討。テンプレート: `~/.claude/TEMPLATE_SKILL.md`

## 割り込み作業とブランチ管理
- 割り込みは git worktree（stash禁止: 識別ミスリスク）。Agent (`isolation: "worktree"`) で自動化可
- 手順: WIPコミット → `git worktree add` → 完了後 `git worktree remove` → 必要に応じmerge/rebase → `git reset HEAD~1` で再開
- 割り込みが現タスクの前提と判明 → 現ブランチを割り込みブランチ上にrebase（develop経由不要）
- PROGRESS.mdに中断スナップショット記録:
  ```
  ## Phase: feature-A [中断]
  中断理由 / 再開条件 / 再開起点 / 未決定事項
  ```
