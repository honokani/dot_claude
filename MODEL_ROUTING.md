# MODEL_ROUTING.md — サブエージェント委譲のモデル振り分け

> 目的: メインセッション（最上位モデル）から Agent/Workflow で委譲する作業を、難度に応じて下位モデルへ振り分けてコストとレイテンシを下げる。品質責任（最終統合・結論）はメインが持つ。「満足度より正確性優先」は変えない — 降格するのは**検証で誤りを捕捉できる作業**だけ。

## 原則
1. **検証可能性で決める**: メイン側で出力を安価に検証できる（照合・形式チェック・grep確認・一次ソースURL付き）作業ほど下位でよい。検証しにくい判断は上位に残す
2. **迷ったら一段上**: 難度判定に迷ったら上のティアへ
3. **失敗は昇格リトライ**: 委譲結果が検証で弾かれたら、同ティア再試行でなく一段上で再実行
4. **fan-out は安く**: 並列委譲はコストが並列数倍。広いスイープほど下位モデル優先
5. **fork は降格不可**: fork はメインモデル固定（`model` 指定は無視される）。降格委譲は新規エージェント＋自己完結プロンプトで
6. **下位ほどプロンプトを具体的に**: コンテキスト非継承のため、判定基準・出力形式・検証手順・既知の罠を明文化して渡す

## 振り分け表
| ティア | `model` 指定 | 目安となる作業 |
|---|---|---|
| Haiku 4.5 | `haiku` | 判断ゼロの機械作業: ファイル列挙、既知フォーマットの抽出、定型変換、存在/日付確認。※context 200K（他は1M）に注意 |
| Sonnet 5 | `sonnet` | 簡単・定型: grep/探索スイープ、既知URLのfetch+抽出・要約、整形・表化、方針確定済みの定型コード変更、bulk-verification の項目単位チェック |
| Opus 4.8 | `opus` | 中難度: 複数ソース照合の調査（検証ルールを明文化して渡す）、仕様確定済みの実装、レビュー/敵対的検証の1票、素材が揃った統合要約、デバッグの一次切り分け |
| メイン（Fable等） | 指定なし | 高難度・検証困難: 設計判断、仕様解釈・曖昧さ解消、ライセンス/権利/安全性の解釈、最終統合と結論の品質責任、難デバッグ |

## 難度判定（上から評価、最初に該当したティアで確定）
1. 設計判断・仕様解釈・ユーザー合意が必要 → **メイン**
2. 出力の正否をメインが安価に検証できない → **Opus 以上**
3. 複数ソースの突き合わせ・矛盾解消を含む → **Opus**
4. 手順を明文化でき、出力形式が固定 → **Sonnet**
5. 判断ゼロの機械作業 → **Haiku**

## 適用方法
- **Agent tool**: `model: "sonnet"` 等を指定（省略時はメインモデル継承 = 降格の意図があるなら明示必須）
- **Workflow**: `agent(prompt, {model: 'sonnet', effort: 'low'})`。effort も難度連動（機械作業=low / 通常=省略 / 最難関の verify・judge=high 以上）
- bulk-verification skill のサブエージェントも本表に従う（項目単位チェックは通常 Sonnet）

## 価格スナップショット（2026-07-02 記録、出典: claude-api skill cache 2026-06-24。変動あり）
| モデル | Input $/1M | Output $/1M | 比率 |
|---|---|---|---|
| Fable 5 | $10 | $50 | 10 |
| Opus 4.8 | $5 | $25 | 5 |
| Sonnet 5 | $3（intro $2 〜2026-08-31） | $15（intro $10） | 3 |
| Haiku 4.5 | $1 | $5 | 1 |

> 更新条件: モデルラインナップ・価格の改定時に本表と振り分け表を見直す。価格は claude-api skill で一次確認（メモリから書かない）。
